# List robots.txt files saved along with the package

list robots.txt files saved along with the package: these functions ar
very handy for testing (not used otherwise)

## Usage

``` r
rt_list_rtxt()
```
